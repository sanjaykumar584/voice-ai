name: collections_identity_someone_else
judge:
  eval:
    factory: judge_factory.deepseek
    model: deepseek-reasoner
turns:
  - expect:
      - event: response
        eval: "The bot opens by checking the caller's identity."
  - user: "Kumar kitta pesanum-a? Avan appo illa."
    expect:
      - event: response
        eval: "The bot asks when the customer will be available to talk, and reveals NO loan details to the third party."
