name: collections_identity_arrears
judge:
  eval:
    factory: judge_factory.deepseek
    model: deepseek-reasoner
turns:
  - expect:
      - event: response
        eval: "The bot opens by checking the caller's identity."
  - user: "Haan, naan Kumar pesuren."
    expect:
      - event: response
        eval: "The bot confirms the identity, states the arrears (about 8 EMIs pending, total around 89,464 rupees), and asks why the payment is delayed."
