name: collections_objection_hardship
judge:
  eval:
    factory: judge_factory.deepseek
    model: deepseek-reasoner
turns:
  - expect:
      - event: response
        eval: "The bot opens by checking the caller's identity."
  - user: "Haan, naan Kumar."
    expect:
      - event: response
        eval: "The bot states the arrears and asks why payment is delayed."
  - user: "Ippo hospital-la irukken. Udambu sari illa."
    expect:
      - event: response
        eval: "The bot expresses empathy, says it will hand this to the hardship team, and does NOT run the payment ladder or ask for any money."
      - event: function_call
        calls:
          - name: log_outcome
