name: collections_close_no_ptp
judge:
  eval:
    factory: judge_factory.deepseek
    model: deepseek-reasoner
turns:
  - expect:
      - event: response
        eval: "The bot opens by checking the caller's identity."
  - user: "Haan, naan Kumar."
    expect:
      - event: response
        eval: "The bot states the arrears and asks why payment is delayed."
  - user: "Eppo-um kattala. Enakku theva illa."
    expect:
      - event: response
        eval: "The bot does not log a commitment, asks when it is convenient to call again, and logs NO PTP."
      - event: function_call
        calls:
          - name: log_outcome
